.DS_Store
node_modules
.worktrees
.sst
.env
.idea
.vscode
.codex
*~
playground
tmp
dist
ts-dist
.turbo
.artifacts/
**/.artifacts/
**/.serena
.serena/
/result
refs
Session.vim
/opencode.json
a.out
target
.scripts
.direnv/

# Local dev files
opencode-dev
UPCOMING_CHANGELOG.md
logs/
*.bun-build
tsconfig.tsbuildinfo
.planning/
.tmp-plan/
.claude/worktrees/
.mcp.json

# Browser/devtool runtime traces
.playwright-mcp/
console-debug.log
*.console.log

# Local secret stash for gpd_mint Slack-app setup (config tokens, bot
# tokens, signing secrets). Lives in /tmp by convention — but if anyone
# ever moves it into the repo, ensure git never sees it.
gpd-mint-secrets.env
**/gpd-mint-secrets.env
*.slack-config.env
*.slack-secrets.env

# Python bytecode (infra/litellm/* test runs)
**/__pycache__/
*.pyc
